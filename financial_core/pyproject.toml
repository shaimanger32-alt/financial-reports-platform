[project]
name = "fri-financial-core"
version = "0.1.0"
description = "Deterministic financial domain logic. No database, no HTTP, no framework."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/financial_core"]
